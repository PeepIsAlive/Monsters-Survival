using UI.Settings;

namespace UI.Views
{
    public sealed class DefaultButtonView : ButtonView
    {
        public void Setup(DefaultButtonSettings settings)
        {
            base.Setup(settings);
        }
    }
}
